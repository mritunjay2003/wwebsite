import { ROUTES } from "../../../utils/constants";

const ActionOptions = [
  { name: "Cart", path: `${ROUTES.HOME}${ROUTES.CART}` },
  { name: "Checkout", path: `${ROUTES.HOME}${ROUTES.CHECKOUT}` },
  { name: "comparison" },
];

export { ActionOptions };
